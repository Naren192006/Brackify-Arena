"use client";
import Link from "next/link";
import { useState } from "react";
import { useQuery } from "@tanstack/react-query";
import { listManagedTournaments } from "@/lib/admin/roles";
export default function AdminTournamentsPage() { const [search,setSearch]=useState(""); const query=useQuery({queryKey:["managed-tournaments"],queryFn:listManagedTournaments}); const tournaments=(query.data??[]).filter((item)=>item.title.toLowerCase().includes(search.toLowerCase())); return <main className="mx-auto max-w-7xl px-4 py-10 sm:px-6"><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="text-sm uppercase tracking-[0.25em] text-arena-accent">Manage</p><h1 className="mt-2 font-display text-4xl font-bold text-white">Tournaments</h1></div><Link href="/admin/tournaments/create" className="btn-primary px-4 py-2">Create tournament</Link></div><input className="input-field mt-6 max-w-sm" placeholder="Search tournaments" value={search} onChange={(event)=>setSearch(event.target.value)} /><div className="mt-6 overflow-x-auto rounded-2xl border border-white/10"><table className="w-full min-w-[720px] text-left text-sm"><thead className="bg-white/[0.04] text-arena-muted"><tr><th className="p-4">Title</th><th>Game</th><th>Status</th><th>Registered</th><th>Start date</th><th>Actions</th></tr></thead><tbody>{tournaments.map((item)=><tr className="border-t border-white/10 text-white" key={item.id}><td className="p-4 font-semibold">{item.title}</td><td>{item.game}</td><td className="uppercase text-arena-accent">{item.status}</td><td>{item.registered_count}/{item.max_teams}</td><td>{new Date(item.start_time).toLocaleDateString()}</td><td><Link className="text-arena-accent hover:underline" href={`/admin/tournaments/${item.slug}`}>Open admin panel</Link></td></tr>)}</tbody></table>{!tournaments.length?<p className="p-8 text-center text-arena-muted">No tournaments found.</p>:null}</div></main>; }
