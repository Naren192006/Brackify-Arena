import type { Team } from "@/types/arena";
import { GlowAvatar } from "@/components/ui/GlowAvatar";
export function TeamPreviewCard({ team, children }: { team: Team; children: React.ReactNode }) { return <article className="liquid-card team-preview-card p-5"><div className="flex items-center gap-4"><GlowAvatar src={team.logo_url} label={team.name} large /><div className="min-w-0"><h3 className="truncate font-display text-2xl font-semibold text-white">{team.name} <span className="text-sm text-arena-accent">[{team.tag}]</span></h3><div className="mt-1 flex flex-wrap gap-2"><span className="glass-badge text-arena-accent">{team.role === "captain" ? "Captain" : "Member"}</span><span className="glass-badge">RP —</span><span className="glass-badge">Trust —</span></div></div></div>{children}</article>; }
