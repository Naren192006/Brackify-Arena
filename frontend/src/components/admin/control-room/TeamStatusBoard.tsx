import type { AdminRegistration } from "@/types/admin";
export function TeamStatusBoard({ registrations }: { registrations: AdminRegistration[] }) { return <div className="space-y-2">{registrations.length ? registrations.map((registration) => <div className="flex flex-wrap items-center justify-between gap-3 rounded-xl bg-white/[0.04] p-3" key={registration.id}><div><p className="font-semibold text-arena-text">{registration.teams?.name ?? "Team"} <span className="text-arena-accent">[{registration.teams?.tag ?? "—"}]</span></p><p className="text-xs text-arena-muted">{registration.teams?.team_members.length ?? 0} members · {registration.status}</p></div><div className="flex gap-3 text-xs uppercase"><span className={registration.checked_in ? "text-arena-success" : "text-arena-muted"}>{registration.checked_in ? "Checked in" : "Pending"}</span></div></div>) : <p className="text-sm text-arena-muted">No registered teams.</p>}</div>; }
