import type { AdminMatch } from "@/types/admin";
export function RoundTracker({ matches }: { matches: AdminMatch[] }) { const rounds = [...new Set(matches.map((m) => m.round_number))].sort((a, b) => a - b); return <div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-4">{rounds.length ? rounds.map((round) => { const items = matches.filter((m) => m.round_number === round); const done = items.filter((m) => m.status === "completed").length; return <div className="rounded-xl bg-white/[0.04] p-4" key={round}><p className="font-display text-lg font-semibold text-white">Round {round}</p><p className="mt-1 text-sm text-arena-muted">{done}/{items.length} completed</p><div className="mt-3 h-1.5 rounded bg-white/10"><div className="h-full rounded bg-cyan-400" style={{ width: `${items.length ? done / items.length * 100 : 0}%` }} /></div></div>; }) : <p className="text-sm text-arena-muted">Generate a bracket to track rounds.</p>}</div>; }
