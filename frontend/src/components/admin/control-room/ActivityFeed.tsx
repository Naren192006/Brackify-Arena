export function ActivityFeed({ items }: { items: { id: string; description: string; created_at: string }[] }) { return items.length ? <div className="space-y-3">{items.map((item) => <div className="border-b border-arena-border pb-3 last:border-0" key={item.id}><p className="text-sm text-arena-text">{item.description}</p><p className="mt-1 text-xs text-arena-muted">{new Date(item.created_at).toLocaleString()}</p></div>)}</div> : <p className="text-sm text-arena-muted">No recent activity.</p>; }
