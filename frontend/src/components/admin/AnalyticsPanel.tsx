import type { AdminAnalytics } from "@/types/admin";

export function AnalyticsPanel({ analytics }: { analytics: AdminAnalytics }) { const items = [["Registered teams", analytics.registeredTeams], ["Approved teams", analytics.approvedTeams], ["Rejected teams", analytics.rejectedTeams], ["Matches completed", analytics.matchesCompleted], ["Matches pending", analytics.matchesPending], ["Current round", analytics.currentRound ?? "—"]]; return <div className="grid grid-cols-2 gap-3 sm:grid-cols-3">{items.map(([label, value]) => <div className="rounded-xl bg-white/[0.04] p-3" key={String(label)}><p className="text-xs text-arena-muted">{label}</p><p className="mt-1 font-display text-2xl font-semibold text-arena-text">{value}</p></div>)}</div>; }
