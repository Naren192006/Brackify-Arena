export function GlowAvatar({ src, label, large = false }: { src?: string | null; label: string; large?: boolean }) { const size = large ? "h-16 w-16 text-2xl" : "h-12 w-12 text-lg"; return src ? <img src={src} alt={`${label} logo`} className={`${size} glow-avatar rounded-full object-cover`} /> : <div className={`${size} glow-avatar flex items-center justify-center rounded-full bg-arena-bg-elevated font-display font-bold text-arena-accent`}>{label.slice(0, 1).toUpperCase()}</div>; }
