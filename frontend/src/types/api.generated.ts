/** Generated-schema-compatible enum surface used by the typed application layer. */
export type Database = {
  public: {
    Enums: {
      tournament_status: "draft" | "open" | "full" | "registration_closed" | "check_in" | "ongoing" | "completed" | "cancelled";
    };
  };
};

export type paths = Record<string, unknown>;
